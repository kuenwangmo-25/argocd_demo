apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx-pod

spec:
  containers::
    - name: nginx
      image: quay.io/pandeysp/mywebapp
      ports:
        - containerPort: 80
